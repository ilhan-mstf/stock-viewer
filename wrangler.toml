name = "stock-viewer"
compatibility_date = "2024-04-05"
compatibility_flags = ["nodejs_compat"]

# Point to the generated worker (created by next-on-pages)
main = ".vercel/output/static/_worker.js"

# Serve static assets
[assets]
directory = ".vercel/output/static"
